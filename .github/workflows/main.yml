- name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm' # This tells GitHub to cache your node_modules

    - name: Install dependencies
      run: npm ci # Use 'npm ci' instead of 'npm install' for faster, consistent builds
